"use strict";
var __extends = (this && this.__extends) || function (d, b) {
    for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p];
    function __() { this.constructor = d; }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
};
var Recognizer_1 = require('./Recognizer');
/**
 * Detects the end of an utterance and should be used in parallel with a
 * CloudRecognizer.
 * ```
 * var asr = require('jibo').asr;
 * var action = asr.createAudioAction();
 * var cloud = new asr.CloudRecognizer();
 * var endOfSpeech = new asr.EndOfSpeechRecognizer();
 * var task = cloud + " | " + endOfSpeech;
 * action.start(task);
 * ```
 * @class EndOfSpeechRecognizer
 * @private
 * @extends {Recognizer}
 * @memberof module:jibo.services.asr
 * @param {Object} options
 */
var EndOfSpeechRecognizer = (function (_super) {
    __extends(EndOfSpeechRecognizer, _super);
    function EndOfSpeechRecognizer(options) {
        options = options || {};
        options.path = "sensory_speech_detector";
        options.speakerId = false;
        _super.call(this, options);
        this.type = 'eos';
    }
    EndOfSpeechRecognizer.prototype.onResult = function (data) {
        if (data.status === 'normal') {
            this.emit('result', data);
        }
    };
    return EndOfSpeechRecognizer;
}(Recognizer_1.default));
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = EndOfSpeechRecognizer;

//# sourceMappingURL=../../map/services/asr/EndOfSpeechRecognizer.js.map
